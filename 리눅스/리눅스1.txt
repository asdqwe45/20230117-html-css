펌웨어
OS
멀티스레드
멀티태스킹
커널
app
shell
vi
gcc
make

리눅스를 배운다
리눅스를 설치한다
 -> 사실 리눅스는 OS가 아니라 , OS 안에 있는 커널이었다.

-> OS : 커널 + shell(=app)
-> window OS : 윈도우용 커널이 안에 있다.

Ubuntu != 리눅스
Ubuntu == 리눅스(커널) + shell
Ubuntu == 리눅스 배포판


리눅스는 서버용

임베디드 OS 
 Non-OS / RTOS / Linux

RTOS : RealTime OS
H/W 시간
  -> 100% 정확도

S/W 시간
  -> 99.9% 정확도

임베디드 제품들 동작하면
  -> 공통적으로 발생하는 현상
     :  발열
  -> S/W 시간은 발열로 인해 부정확해질 가능성이 높아진다.

  -> CPU / MCU / MPU
     : 내부적으로 clock (시계)
     : 열이 나면 전자의 이동속도가 달라진다.


설치할 때 주의사랑
계정 : 본인이름 (jm)
비밀번호 : 1


컴퓨터를 다룬다
 -> 웹서핑
 -> 폴더 만들고 과제 / ppt 한글 word 작업하고
저장
 -> 코딩해서 빌드해서 .cpp 파일 만들어서 저장

리눅스에서 shell 이 뭐지? 처음 들었는데?
 -> 이미 우리는 win에서 shell을 다루고 있었다.

유저는 커널을 직접 다루지 않고 shell 을 이용해서 다루고 있었다.

Shell : 유저가 커널을 다룰 수 있도록 도와주는 인터페이스.  시스템 사용자와 커널과의 인터페이스다.
User Interface : 

Shell은 프로그램이다. (=프로세스)

프로그램 : 0/1로 구성된 파일
프로세스 : 프로그램을 실행한 결과







